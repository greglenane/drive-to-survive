name: s3_data
type: s3
options:
    # Use 'env.' prefix so Evidence pulls from the GitHub runner environment
    accessKeyId: env.S3_ACCESS_KEY_ID
    secretAccessKey: env.S3_SECRET_ACCESS_KEY
    region: env.S3_REGION
    # This helps DuckDB resolve the S3 paths correctly
    provider: aws
